---
name: write-commit
description: 提交信息规范——中文主题 + conventional-commit 格式(type/scope 词表、正文何时写、常见误区)。当要写 git commit 信息、提交代码时使用。
allowed-tools:
  - Bash(git add *)
  - Bash(git commit *)
  - PowerShell(git add *)
  - PowerShell(git commit *)
---

读仓库根目录的 `skills/write-commit.md` 并严格按它执行。它是单一真源,本文件只是入口包装。
