---
name: replace-service
description: 替换/扩展 SmartAdmin 内置服务——DI 整体替换、子类覆写单步、禁用模块+接管路由、消费者种子数据,以及前端用同名页面 key / 语言包覆盖内置页与文案。当用户要定制登录流程、换密码哈希/缓存/存储、覆写内置行为、替换内置前端页面时使用。
---

读仓库根目录的 `skills/replace-service.md` 并严格按它执行。它是单一真源,本文件只是入口包装。
