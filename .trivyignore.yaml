# 镜像扫描的例外。每一条都要写清楚「为什么不可达」与「什么时候复查」——
# 没有失效日期的例外会变成永久的,那时它挡住的就不再是噪声而是真问题。
# 到期后 Trivy 重新把它算进去,CI 判红,逼一次人工复查。

vulnerabilities:
  - id: CVE-2026-56854
    statement: >
      golang.org/x/crypto/ssh 的认证绕过(未强制 source-address 限制),来自上游 caddy:2-alpine
      基础镜像里的 Caddy 二进制,不是本仓的代码。这里的 Caddy 只做静态文件服务与反向代理,
      标准构建里根本没有 SSH 应用,那段代码在我们的配置下不可达。
      修复要等上游用 x/crypto 0.55.0 重新构建镜像:2026-06-24 发布的 2.11.4-alpine(当前 2-alpine
      指向的那个)仍带 0.52.0,我们这边没有别的办法。
      复查:到期时先看 caddy:2-alpine 是否已经带上修复版本,是就删掉这一条。
    expired_at: 2026-12-01
